#include "pch.h"
#include "../dictionary/dictionary.cpp"
#include "../dictionary/dictionary.h"
TEST(TestCaseName, TestName) {
  EXPECT_EQ(1, 1);
  //EXPECT_TRUE(true);
}

TEST(TestAss, Ass)
{
	EXPECT_EQ(0, 0);
}